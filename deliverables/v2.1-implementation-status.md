# 灵枢 V2.1 实施状态

## 已完成

| 模块 | 当前能力 | 验收状态 |
|---|---|---|
| 外部渠道 | 独立配置、掩码回写、平台真实鉴权、连接状态记录 | 已完成首批闭环 |
| GitHub 集成 | API 身份验证、仓库自动发现、API 主机约束 | 已完成首批闭环 |
| 远程实例 | HTTPS 健康检查、Token、超时、重启与日志接口 | 已完成首批闭环 |
| Workflow | 后台 Run、轮询、暂停、恢复、取消、跳过、节点重试、断点运行 | 已完成运行控制 |

## 保留

- 现有实例、Git 集成、Workflow JSON 数据继续兼容。
- 现有 Workflow 同步运行 API 和 Automation 调用方式保持不变。
- 外部渠道仍复用本机灵枢配置文件，但改为后端按单个渠道安全合并。

## 后续小版本

1. 渠道回调验签、入站消息队列、出站发送审计和失败重试。
2. GitHub Issue、Pull Request、Release、Actions 与 Webhook 触发器。
3. 远程实例能力协商、实例级授权和断线重连观测。
4. Workflow 实时 SSE 日志、持久化运行队列及统一 Agent/Skill/Tool 执行器。
